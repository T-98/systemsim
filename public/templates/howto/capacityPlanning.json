{
  "systemsimVersion": "1.0",
  "metadata": {
    "name": "Capacity Planning",
    "source": "How-to — size a system from a guess, then prove it wrong",
    "tags": ["howto", "capacity", "bote", "estimation"]
  },
  "nodes": [
    { "type": "load_balancer", "label": "Edge LB", "config": { "isEntry": true } },
    { "type": "server", "label": "App Server", "config": { "instanceCount": 2, "processingTimeMs": 50, "maxConcurrent": 500 } },
    { "type": "database", "label": "Primary DB", "config": { "engine": "postgres", "writeThroughputRps": 50, "readThroughputRps": 200, "connectionPoolSize": 20 } }
  ],
  "edges": [
    { "source": "load_balancer-0", "target": "server-1" },
    { "source": "server-1", "target": "database-2" }
  ]
}
